/**
 * Определяет тип объекта с помощью единственного свойства:
 * selectorCounter: строковое свойство, представляющее CSS-селектор для элемента счетчика.
 */

export type TBasketShortSettings = {
	selectorCounter: string;
};
