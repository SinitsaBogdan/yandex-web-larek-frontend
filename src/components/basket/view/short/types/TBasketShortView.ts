/**
 * Это определение типа, `TBasketShortView`, представляет объект с единственным свойством:
 * count: число, представляющее количество элементов в корзине (краткое представление).
 */
export type TBasketShortView = {
	count: number;
};
