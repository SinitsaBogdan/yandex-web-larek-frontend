/**
 * Определение типа, `TPageview`, определяет тип объекта с единственным свойством:
 * @param locked - логическое свойство, указывающее, заблокирован просмотр страницы или нет.
 */
export type TPageView = {
	locked: boolean;
};
